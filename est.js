/**
 * This function checks if the user has permission to perform a certain action.
 * Only specific roles are allowed.
 *
 * @param {Object} user - The user object.
 * @returns {boolean} True if the user is allowed, otherwise false.
 */
function isUserAllowed(user) {
    // Implementation code here
}
